import Image from 'next/image'; import Link from 'next/link'; import { getCollections } from '../../lib/commerce';
export default async function Collections(){const collections=await getCollections();return <section className="page-shell"><div className="page-title"><span>EXPLORE</span><h1>SHOP BY COLLECTION</h1></div><div className="collection-grid">{collections.map(c=><Link key={c.slug} href={`/collections/${c.slug}`}><div className="collection-image"><Image src={c.image || '/images/products/the-number-one/1.webp'} fill alt={c.title}/></div><h2>{c.title}</h2></Link>)}</div></section>}
