export const metadata={title:'Account'}; export default function Account(){const accountUrl=process.env.NEXT_PUBLIC_SHOPIFY_ACCOUNT_URL;return <section className="account-page"><span>CUSTOMER ACCOUNT</span><h1>ACCOUNT</h1><p>View orders, manage your details and use a faster checkout experience.</p>{accountUrl?<a className="black-button inline" href={accountUrl}>SIGN IN / CREATE ACCOUNT</a>:<button className="black-button inline" disabled>CUSTOMER ACCOUNTS AVAILABLE AT LAUNCH</button>}</section>}
