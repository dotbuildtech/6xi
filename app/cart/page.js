'use client';
import Image from 'next/image'; import Link from 'next/link'; import { useCart } from '../../components/CartProvider';
const money=n=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(n);
export default function CartPage(){const {items,total,remove,setQty,checkout,checkoutBusy,checkoutError}=useCart();return <section className="page-shell cart-page"><div className="page-title"><span>ORDER</span><h1>YOUR CART</h1></div>{!items.length?<><p>Your cart is empty.</p><Link className="black-button inline" href="/collections/new-arrivals">SHOP NEW ARRIVALS</Link></>:<div className="cart-layout"><div>{items.map(i=><div className="cart-page-line" key={i.key}><Image src={i.image} width={140} height={175} alt=""/><div><Link href={`/products/${i.slug}`}><h3>{i.title}</h3></Link><p>Size {i.size}</p><strong>{money(i.price)}</strong><label>Qty <input type="number" min="1" value={i.qty} onChange={e=>setQty(i.key,Number(e.target.value)||1)}/></label><button className="text-button" onClick={()=>remove(i.key)}>Remove</button></div></div>)}</div><aside className="cart-summary"><h2>ORDER SUMMARY</h2><div><span>Subtotal</span><strong>{money(total)}</strong></div><p>Shipping and taxes calculated at checkout.</p>{checkoutError&&<p className="checkout-error">{checkoutError}</p>}<button className="black-button full" disabled={checkoutBusy} onClick={checkout}>{checkoutBusy?'CREATING CHECKOUT…':'CHECKOUT'}</button></aside></div>}</section>}
